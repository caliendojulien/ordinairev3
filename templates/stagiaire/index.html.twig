{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/stagiaire/index.css') }}">
{% endblock %}

{% block title %}
    Décompte repas
{% endblock %}

{% block body %}
    <div id="calendrier">
        {% for c in calendar %}
            <a href="{{ path('stagiaire_decompte', {'month':  c | date('m') , 'year': c | date('Y')}) }}"><p>{{ c | format_datetime(pattern='MMMM YYYY', locale='fr') }}</p></a>
        {% endfor %}
    </div>
{% endblock %}

